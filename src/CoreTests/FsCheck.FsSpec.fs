module GeneratorExperiment

module Gen = 
    open FsSpec.CustomTree
    open FsCheck
    //let fromConstraint (constraints:Constraint<'a>) : Gen<'a> =
    //   Gen.
    //   Arb.generate<'a> |> 
    //   Arb.